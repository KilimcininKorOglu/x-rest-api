# The ONLY configuration that comes from the environment.
# Everything else (accounts, API keys, proxy, writes, ...) is managed in /admin.

# Listen port (binds 0.0.0.0). Default 8430.
PORT=8430

# Optional: SQLite database path. Default data/x-rest-api.db
# DB_PATH=data/x-rest-api.db
